version: "3.9"

services:
  jupyter:
    build: .
    container_name: ds_jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./data:/data
      - ./work:/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
